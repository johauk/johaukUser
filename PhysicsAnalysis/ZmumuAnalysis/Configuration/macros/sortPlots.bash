#!/bin/bash


STEP="2b"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="2"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="3d"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="3e"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="3f"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="3"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="4a"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="4b"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="4c"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="4d"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="4"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="5"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="6a"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="6"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done





STEP="ZVetoLow3f"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVetoLow3"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVetoLow4d"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVetoLow4"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVetoLow5"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVetoLow6a"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVetoLow6"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done





STEP="ZVetoHigh3f"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVetoHigh3"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVetoHigh4d"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVetoHigh4"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVetoHigh5"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVetoHigh6a"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVetoHigh6"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done


















