#!/bin/bash


STEP="2b"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="2"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="3d"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="3e"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="3f"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="3"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="4a"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="4b"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="4c"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="4d"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="4"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="5"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="6a"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="6"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done







STEP="ZVeto3f"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVeto3"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVeto4d"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVeto4"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVeto5"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVeto6a"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done

STEP="ZVeto6"
STEPDIR="plots/Step${STEP}/"; mkdir $STEPDIR; for file in plots/*Step${STEP}*.png; do mv $file $STEPDIR; done
















